name: Android Build and Release

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------
      # Checkout
      # ---------------------------
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # ---------------------------
      # Java (for Android)
      # ---------------------------
      - name: ☕ Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: gradle

      # ---------------------------
      # Node (NO npm cache)
      # ---------------------------
      - name: 📦 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # ---------------------------
      # Generate lock file EVERY TIME
      # ---------------------------
      - name: 📚 Generate package-lock.json & install deps
        run: |
          npm install
          echo "✅ package-lock.json generated"

      # ---------------------------
      # Android permissions
      # ---------------------------
      - name: 🔨 Grant execute permission for gradlew
        run: chmod +x android/gradlew

      # ---------------------------
      # Build APK
      # ---------------------------
      - name: 🏗️ Build APK
        run: |
          cd android
          ./gradlew assembleRelease --no-daemon --stacktrace

      # ---------------------------
      # Build metadata
      # ---------------------------
      - name: 📝 Get build info
        id: build-info
        run: |
          echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
          echo "COMMIT_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "BUILD_DATE=$(date +'%Y-%m-%d_%H-%M-%S')" >> $GITHUB_OUTPUT
          echo "BRANCH=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT

      # ---------------------------
      # Rename APK
      # ---------------------------
      - name: 📦 Rename APK
        run: |
          cd android/app/build/outputs/apk/release
          if [ -f app-release-unsigned.apk ]; then
            mv app-release-unsigned.apk medical-scheduler-v${{ steps.build-info.outputs.VERSION }}-${{ steps.build-info.outputs.COMMIT_SHORT }}.apk
          else
            mv app-release.apk medical-scheduler-v${{ steps.build-info.outputs.VERSION }}-${{ steps.build-info.outputs.COMMIT_SHORT }}.apk
          fi

      # ---------------------------
      # Upload artifacts
      # ---------------------------
      - name: 📤 Upload APK (Latest)
        uses: actions/upload-artifact@v4
        with:
          name: MedicalScheduler-Latest
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: 📤 Upload APK (Versioned)
        uses: actions/upload-artifact@v4
        with:
          name: MedicalScheduler-v${{ steps.build-info.outputs.VERSION }}-${{ steps.build-info.outputs.BUILD_DATE }}
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 90

      # ---------------------------
      # Summary
      # ---------------------------
      - name: 📊 Build Summary
        run: |
          echo "## ✅ Build Successful!" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** ${{ steps.build-info.outputs.VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ steps.build-info.outputs.COMMIT_SHORT }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ steps.build-info.outputs.BRANCH }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Lock File:** Generated on this run" >> $GITHUB_STEP_SUMMARY

  notify:
    needs: build
    runs-on: ubuntu-latest
    if: success()

    steps:
      - name: 📢 Build Success Notification
        run: |
          echo "================================================"
          echo "✅ BUILD SUCCESSFUL!"
          echo "📱 App: Medical Class Scheduler"
          echo "🔗 Link: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "================================================"
